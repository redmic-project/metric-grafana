apiVersion: 1

providers:
  - name: 'default'
    orgId: ${GF_USERS_AUTO_ASSIGN_ORG_ID}
    folder: ''
    type: file
    disableDeletion: false
    allowUiUpdates: true
    options:
      path: ${DASHBOARDS_PATH}
